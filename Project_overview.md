I've explored the project. Now let me give you a beginner-friendly explanation in Hindi.

---

## प्रोजेक्ट का नाम

**"Practical MLOps using Azure"** — यह प्रोजेक्ट एक भारतीय डेटा साइंटिस्ट **Anupam** ने बनाया है, और इसे **Towards Data Science** पर एक आर्टिकल के रूप में भी पब्लिश किया गया है।

## प्रोजेक्ट क्या करता है? (आसान भाषा में)

सीधे शब्दों में कहें तो, यह एक **पूरी तरह से ऑटोमेटेड सिस्टम** है जो **स्टॉक मार्केट की कीमतों को predict (भविष्यवाणी)** करता है, और इसे बार-बार अपने आप अपडेट (retrain) करता रहता है — बिना किसी इंसान के मैन्युअल दखल के।

इसे "**MLOps**" (Machine Learning + Operations) कहते हैं, यानी सिर्फ एक बार मॉडल बनाकर बैठ जाना नहीं, बल्कि उसे लगातार **डेटा अपडेट → रीट्रेनिंग → डिप्लॉयमेंट** के साइकल में रखना।

## वर्कफ़्लो (Workflow) — 3 मुख्य पाइपलाइन (Pipelines)

यह पूरा सिस्टम **GitHub Actions cron jobs** (यानी समय पर अपने आप चलने वाले शेड्यूल्ड टास्क) से हफ्ते में एक बार चलता है:

### 1️⃣ Data Pipeline (डेटा पाइपलाइन)
- **yfinance** नाम की लाइब्रेरी से स्टॉक का डेटा डाउनलोड होता है (जैसे इस प्रोजेक्ट में `ONGC.NS`, `WIPRO.NS`, `LT.NS` — ये भारतीय (NSE) कंपनियों के स्टॉक टिकर हैं)
- डेटा को साफ करके CSV फाइल में सेव किया जाता है
- फिर उसे GitHub repo में पुश किया जाता है और **Azure ML Studio** के डेटास्टोर में रजिस्टर किया जाता है
- **फाइलें:** `jobs/data_download.py`, `jobs/data_upload.yml`, `jobs/update_yamls.py`

### 2️⃣ Training Pipeline (ट्रेनिंग पाइपलाइन)
- Azure पर एक "compute" (यानी वर्चुअल कंप्यूटर) बनाया जाता है
- उस पर **PyTorch Lightning** का उपयोग करके एक **LSTM मॉडल** ट्रेन होता है (LSTM एक खास तरह का न्यूरल नेटवर्क है जो टाइम-सीरीज़ डेटा, जैसे स्टॉक कीमतों, के लिए अच्छा होता है)
- ट्रेन किया हुआ नया मॉडल Azure ML वर्कस्पेस में रजिस्टर (सेव) कर दिया जाता है
- **फाइलें:** `jobs/train.py`, `src/training_script.py`, `src/train.py`

### 3️⃣ Deployment Pipeline (डिप्लॉयमेंट पाइपलाइन)
- नया ट्रेन किया गया मॉडल एक **Online Endpoint** पर डिप्लॉय हो जाता है (यानी अब कोई भी उस मॉडल को API के ज़रिए इस्तेमाल कर के prediction ले सकता है)
- अगली बार जब भी नया मॉडल ट्रेन होता है, वो उसी endpoint पर अपडेट हो जाता है (नया endpoint बनाने की ज़रूरत नहीं)
- **फाइलें:** `jobs/deploy.yml`, `jobs/deployment.py`

## पूरा साइकल (सरल डायग्राम की तरह)

```
हर हफ्ते (Cron Job) 
      ↓
डेटा डाउनलोड (yfinance से स्टॉक डेटा) 
      ↓
Azure Datastore में डेटा अपलोड
      ↓
LSTM मॉडल की ट्रेनिंग (PyTorch)
      ↓
मॉडल को Azure ML में रजिस्टर करना
      ↓
Online Endpoint पर डिप्लॉय करना
```

## प्रोजेक्ट में इस्तेमाल हुई टेक्नोलॉजी
- **Azure ML SDK v2** — मॉडल ट्रेनिंग और डिप्लॉयमेंट के लिए
- **PyTorch / PyTorch Lightning** — LSTM मॉडल बनाने के लिए
- **yfinance** — स्टॉक डेटा लेने के लिए
- **GitHub Actions** — पूरे प्रोसेस को ऑटोमेट (cron jobs) करने के लिए

## एक ज़रूरी बात

README के हिसाब से, **इस प्रोजेक्ट के सारे cron jobs अभी रोक (paused) दिए गए हैं और endpoint को डिलीट कर दिया गया है**, ताकि Azure का बिल (cost) न बढ़े। यानी यह अभी सिर्फ एक डेमो/लर्निंग प्रोजेक्ट के तौर पर मौजूद है, लाइव नहीं चल रहा।
